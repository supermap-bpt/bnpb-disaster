"""Placeholder for Task 5's FloodJobRepository.

This stub exists only so that `app.services.flood`'s module-level import
resolves during Task 3's own tests (which monkeypatch `_update_job` entirely
and never construct this class). Task 5 must replace this with a real
repository implementation (mirroring app.db.landslide_repository's
LandslideJobRepository), including CRUD/update methods backed by the
FloodJob model.
"""


class FloodJobRepository:
    pass
